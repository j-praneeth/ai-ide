# ── Mobile App Configuration (set before building APK) ────────────────
# The relay server URL is baked into the app at build time.
# Users only need to enter a room code.

VITE_RELAY_URL=https://your-relay.up.railway.app
